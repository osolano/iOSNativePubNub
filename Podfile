# Uncomment the next line to define a global platform for your project
 platform :ios, '8.0'

target 'NativePubNubChat' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod "PubNub", "~> 4"
  # Pods for NativePubNubChat

  target 'NativePubNubChatTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'NativePubNubChatUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
